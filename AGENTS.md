# Config Mate UI 维护规范

## 项目定位

Config Mate 是现场部署控制台，界面应当服务于高频运维操作：清晰、克制、稳定、可扫描。不要把页面做成营销站、展示页或装饰型大屏。

## UI 基座

- 当前项目不直接引入 React 版 shadcn/ui。
- 借鉴 shadcn/ui 的设计系统方式：先使用语义化 token，再使用统一组件类，最后才写模块局部样式。
- 基础样式入口：
  - `assets/styles/tokens.css`：颜色、圆角、阴影、间距、字体、状态色。
  - `assets/styles/base.css`：全局基础样式。
  - `assets/styles/components.css`：按钮、输入框、卡片、弹窗、状态等通用组件。
  - `assets/styles/layout.css`：页面结构和工作台布局。

## 约束

- 不要在新增 UI 中随手写死颜色，例如 `#2563eb`、`rgba(...)`。优先使用 `--cm-*` token。
- 不要新增一套按钮体系。优先复用 `.btn`、`.btn-primary`、`.btn-danger`、`.btn-ghost` 或扩展现有组件语义。
- 不要新增一套卡片/弹窗/状态徽章样式。先看 `components.css` 是否已有语义。
- 不要使用夸张渐变、大面积高饱和颜色、营销式 hero、装饰性图形。
- 页面模块可以有局部样式，但必须建立在 token 和通用组件之上。
- 修改 UI 时不得改变接口、鉴权、Docker 操作、清理逻辑、安装逻辑等业务行为。
- 非明确 UI 改版需求，不允许自动更新 Playwright 或 BackstopJS 基准截图。
- 不允许为了让测试通过而随意扩大、放宽或删除 UI 一致性测试规则。
- 不允许随意修改全局样式入口：`tokens.css`、`base.css`、`components.css`、`layout.css`。确需修改时，必须说明影响范围，并优先证明局部样式无法满足需求。
- 不允许引入新的 UI 风格、颜色体系、圆角体系、阴影体系或按钮/表单/弹窗体系。
- 优先复用已有组件类、`--cm-*` 样式变量和当前设计规范；只有确实缺失时才补充语义化组件样式。

## 推荐工作流

1. 先确认要改的是哪一块：服务编排、日志、历史版本、安装弹窗、业务配置表单。
2. 先复用 token 和通用组件；只有确实缺失时才新增组件语义。
3. 每次只迁移一个页面区域，提交粒度保持小。
4. 修改后先做最小自检，例如受影响 JS 文件执行 `node --check`，再执行完整验证门禁。

## 强制验证门禁

每次开发完成后，AI 必须自动执行 UI 一致性验证和构建验证。不得只给出“建议执行”的命令。

必须执行：

- `npm run lint:style`
- `npm run test:ui`
- `npm run test:visual`
- `npm run build`

补充要求：

- `npm run test:visual` 需要本地服务可访问时，先启动项目服务，例如：
  `NO_BROWSER=1 PORT=3311 CONFIG_MATE_PASSWORD=123456 node tb-config-src.js --dev`
- 如果 UI 测试失败，必须先分析失败截图、差异报告、失败选择器和最近改动，再修复导致差异的代码或测试稳定性问题。
- UI 测试失败时，不允许直接执行 `npm run test:ui:update`、`npm run test:visual:init` 或 `npm run test:visual:approve` 来掩盖问题。
- 只有当用户明确要求 UI 改版、视觉调整、更新基准图，或确认当前视觉差异为预期结果时，才允许更新或批准基准截图。
- 如果因环境限制无法执行某条命令，必须在最终回复中明确说明原因、已执行的替代验证，以及剩余风险。

## 最终输出要求

每次完成开发后，最终回复必须包含测试结果报告，至少列出：

- 已执行的命令。
- 每条命令的通过/失败结果。
- UI 差异是否存在。
- 如果存在失败，说明根因、修复动作和复测结果。
- 是否更新了基准截图；如更新，必须说明用户授权来源。

## 视觉方向

- 背景：浅冷灰蓝。
- 主色：克制蓝色，表达可操作和选中。
- 成功/警告/危险：只用于状态和风险动作。
- 圆角：默认 8px，面板 10-14px，胶囊状态可使用 999px。
- 字体：系统无衬线；代码和日志使用等宽字体。
- 密度：运维工具优先信息密度，不要过度留白。
