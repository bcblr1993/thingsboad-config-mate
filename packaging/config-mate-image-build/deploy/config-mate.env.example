DEPLOY_ROOT=/opt/sprixin-iotcloud
CONFIG_MATE_PORT=3300
CONFIG_MATE_PASSWORD=请修改为现场登录密码

# 一般不需要填写，程序会根据 services/iotcloud 或 services/iotedge 自动识别。
# 如果现场同时存在 iotcloud 和 iotedge，可以明确指定 CLOUD 或 EDGE。
APP_TYPE=
