## [未发布]

### 💄 界面 (UI)
按现场逐页走查结论做的版式整改，全部为样式层改动，未触碰任何业务逻辑。

- **配置页标签与输入框之间的 612px 空白**：左列吃掉全部剩余宽度，输入框又被
  `justify-self:end` + `max-width:430px` 钉在右侧，长 jdbc 串反而显示不全，
  行高固定 108px 一屏只看得到 4 项。改为左列限宽、右列铺满：空白 612→183px，
  输入框 422→813px，行高 113→70px。
- **服务卡片「…」被挤到第二行**：按钮区放不下四个按钮，同行卡片高度在 148/169
  之间参差。改为不换行 + 「…」固定窄宽，网格改 `align-items: stretch` 同行等高。
- **最小字号 9px**：徽章与指标名散落 9px/10px 共 24 处，现场读不清。新增
  `--cm-text-2xs: 11px` 档并全部收口，补用例守住可读下限。
- **页面标题与 KPI 数字同为 26px/800**：标题退到 22px，拉开主次。
- **宽屏无最大宽度**：1920 下内容拉满整屏，现加 `max-width: 1600px` 居中。
- **设计体系收敛**：圆角 13 种、过渡时长 7 种全部落到令牌（页面样式中硬编码归零）；
  97 处字号改为令牌引用。安装页四张指标卡的数值字号由 18/22/26/26 统一到 22px。
- **动效与可访问性**：新增 `prefers-reduced-motion` 支持；按钮补 `:active` 位移
  反馈（现场多为远程桌面，无即时反馈会导致重复点击）、`:focus-visible` 焦点样式
  与 `:disabled` 视觉状态。

### ⚡️ 性能 (Performance)
- 服务状态刷新从约 8 秒降到 0.9 秒。真机逐条计时定位到两个原因：
  - `docker stats --no-stream` 单次要 1.5–2 秒（必须采样 CPU 间隔，与容器数量
    几乎无关），原先按服务逐个 await，把 `/api/services` 拖到 3 秒以上，而其余
    探测全部并行只要 125ms。改为一次批量采样 + 缓存，读取永不阻塞，并发读取
    合并为一次采样。代价是 CPU / 内存最多滞后 8 秒；运行/停止状态仍每次实时探测。
  - 前端 `refreshDeployment` 把 settings / cluster / deployment / plan / services
    五次往返串行 await，改为按真实依赖分两批并发。
  - `haProbe.discover()` 的串行 for-await 改为并行。
- 实测：`/api/services` 3.07s → 0.47s，`/api/plan` 4.06s → 0.59s，
  整体刷新 8.05s → 0.96s。

### 🐛 修复 (Fixed)
合并后在真机（10.8.8.157 / 10.8.8.235）逐项复验时发现的一批缺陷，共同特征
是「机制存在但实际不生效」或「数据到达前先编一个具体值」。

- 修复跨节点依赖在安装页被误报为缺失：依赖装在对端机器上跑着，安装页仍提示
  「请先启动 Redis」，而实际操作并不拦截，界面指引与实际行为相反。根因是同一
  判断有两份独立实现，现合并为唯一实现，结构上不再可能分叉。
- 修复瀚高 License 到期监控从未真正读到过 License：探测在数据目录下找
  `./hgdb.lic`，而 `hg_lic --help` 明确说明 License 在 `$HGDB_HOME/etc/lic/`，
  正常授权的现场也读不到，30 天 / 7 天两级预警形同虚设。同时把 `hg_lic` 自身的
  报错原文带到详情页，直接指明该往哪放。
- 修复资源缓存版本号漏更新：版本计算对 `assets/modules` 等目录取 mtime，而目录
  mtime 只在增删条目时更新，就地改文件内容版本号不变，浏览器继续用旧缓存。改为
  遍历资源树逐文件取 mtime。
- 修复集群模式下总览服务数翻倍：`总览 → 服务管理 → 30 秒内回总览`，服务总数从
  10 变成 20、每个服务出现两次。根因是本机接口与聚合接口共用一个变量。
- 修复安装页在边缘端显示云端：直接打开 `#/install` 时部署信息尚未返回，代码按
  CLOUD 兜底，页面显示「执行 IoT Cloud 的安装初始化任务」和
  `iotcloud/docker-compose-install.yml`。服务管理面包屑、登录页部署类型、顶部
  模式徽标同样存在该兜底，一并改为未知时给占位。
- 修复总览副标题永远停在「正在读取部署环境...」、依赖数先显示服务总数再跳变。
- 修复集群模式下服务卡片标题被徽章挤没：标题行 nowrap 且徽章不收缩，叠加节点
  徽章后 PostgreSQL 只剩「P...」，瀚高双机热备的标题宽度直接是 0px。

### 💄 界面 (UI)
- 总览页标题由「集群总览」改为「部署总览」，并在集群启用时标注服务计数的范围为
  「仅本节点」——该页统计的始终是本节点，跨节点聚合在服务管理页。
- 「已停止 8」等省略列表补出总数（`postgres, postgres-ha 等 8 个`），此前只列
  前两个，会被读成只停了这两个。
- 移除「校验配置」按钮上的常亮告警徽标：它不反映任何状态，恒亮的告警信号会让
  运维学会忽略真正的告警。

### ✅ 验证 (Verified)
- 瀚高双机热备完整生命周期在合并后的构建上复验：备节点加入并流式复制、主动切换
  16s（VIP 从 157 的 `ens3` 漂移到 235 的 `eth0`）、原主库自动降级重新加入 9s、
  切换前后数据双向一致、两端界面各自正确识别「本机」与主备角色。
- 安全边界从 10.8.8.235 这一真实来源复验：5 次错误口令按 IP 锁定 300s、锁定期内
  正确口令同样拒绝、伪造 `X-Forwarded-For` / `X-Real-IP` / `X-Client-IP` /
  `Forwarded` 均无法绕过、锁定按 IP 隔离、集群 token 等长篡改与集群 id 不符均被
  拒绝、持正确集群 token 无法越权做任何管理操作。
- 节点离线降级：单节点不可达时其余节点不受影响，请求耗时有界（2.0s），且不会把
  「看不见」当作依赖已满足；节点恢复后自动回到非降级状态。
- IoTDB 依赖解析复验，含历史与最新分别选用 Cassandra 与 IoTDB 时两者都必须就绪。

## [2.2.0] - 2026-09-10

本版本解决两个现场瓶颈：双机热备无法纳管、服务分散在多台机器上时工具不可用。

### ✨ 新增 (Added)
- 新增双机热备只读纳管：自动发现 `postgres-ha` / `highgo-ha` 容器，在服务卡片上展示 PRIMARY/STANDBY 角色、VIP 归属，详情页展示 repmgr 集群拓扑、流复制延迟和容器生效配置。不修改 HA 交付镜像及其内部脚本，只做外部观测。
- 新增瀚高 License 到期监控：剩余 30 天内在服务卡片提示，7 天内升级为红色告警。
- 新增 Redis Cluster 只读纳管：按容器名发现动态生成的集群，展示节点列表与 `cluster info` 健康状态。
- 服务列表接口新增 `conflicts` 字段，标识与已发现 HA 互斥的单机服务。
- 依赖检查改为按能力解析：数据库能力可由 `postgres` / `postgres-ha` / `highgo-ha` 提供，缓存能力可由 `redis` / `redis-cluster` 提供，组内任一就绪即视为满足。现场把单机数据库换成 HA 后不再被误拦。
- 新增严格依赖校验开关（默认开启）：关闭后依赖未就绪只在确认弹窗提示、不阻断，用于 Kafka 集群、Cassandra 集群等本工具白名单无法穷举的部署形态。
- 新增 Agent / Console 集群模式（阶段一，只读聚合）：每个节点部署同一份 Config Mate，`conf/nodes.yml` 存在即启用 Console 角色；Console 汇总各节点的服务状态，依赖判定先查集群再查本机。据此支持 PostgreSQL、Redis、Kafka、Cassandra 分散在两台或三台机器上的部署形态。
- 节点离线降级：单节点探测失败不影响其余节点，界面标注离线并保留上次已知状态。

### 🛡️ 安全 (Security)
- 管理口令改为 scrypt 加盐哈希存储（`auth.json`，权限 0600），校验使用恒定时间比较；仍在使用默认口令时界面持续提示。
- 新增登录失败锁定：按来源 IP 计数，超阈值临时锁定。
- 修复来源 IP 可被 `X-Forwarded-For` 伪造从而绕过登录锁定的问题：默认只取 TCP 连接的真实来源，反向代理场景需显式开启 `trustProxy`。
- 集群节点间通信使用独立 token，与管理口令分离。
- 请求体超限改为返回 413 并停止读取，此前直接销毁连接导致客户端只看到 `HTTP 100` 而无法得知原因。
- 过期会话改为定时清理，此前只在访问时被动淘汰，长期运行会驻留已过期会话。
- 只读纳管服务禁止通过界面启停：HA 启动有严格的先主后备顺序，错序会触发备库全量 clone。
- 只读纳管服务禁用一键清理：HA 数据在 docker named volume 中，清理白名单的 bind mount 路径对其无效，放行只会归档出空目录并给出虚假的安全感。

### 🐛 修复 (Fixed)
- 修复集群拓扑把对端标成「本机」的问题：原按 repmgr 的 `*` 标记判定，但备库上 repmgr 连接的仍是主库，改为比对容器自身的 `NODE_NAME`。
- 修复 VIP 检测只查 `eth0` 的问题，改为遍历全部网卡。真机网卡名为 `ens3`，原实现在现场必然漏报。
- 修复 repmgr 状态列的可达性标记（`*` / `-` / `?`）被当作状态文本显示的问题。
- 修复安装页把互斥服务同时列为缺失的问题：现场跑着瀚高 HA，界面却提示「请先启动 PostgreSQL 双机热备」，让运维去启动一个根本没部署的服务。`plan.missingDependencyIds` 改为与依赖检查同口径、按能力组判定。
- 修复跨节点依赖判定误报：本机存在 HA 容器时会把单机 `postgres` 从候选中剔除，导致对端跑单机 `postgres` 的部署形态被判为缺失。

### 💄 界面 (UI)
- 服务卡片主标题改为显示服务名称、副标题显示 id，此前主标题显示 id，`postgres-ha` 这类较长 id 被截断成「postgre...」；总览页与服务页统一为同一层级规则。
- 「启动依赖」标签去重（原有星号前缀、标签内星号、文字三处重复表达），并由危险红改为中性描边——它表示「这是必需依赖」而非「出错了」。
- 依赖标签按能力聚合，一组只出一个；互斥候选全部未运行时按能力名提示。
- 后端容器归属诊断由英文原文透传改为中文可操作说明。
- 前端资源缓存版本号改为服务端按文件修改时间自动注入，替代手写 `?v=`。手写版本串漏改会让浏览器继续使用旧缓存——容器已重建而界面仍是旧逻辑，2.0.3 曾因此出过事故。

### 📝 文档 (Docs)
- 新增《HA 交付包已知缺陷》，记录 `pg_isready` 未带端口导致自定义 `PG_PORT` 必然启动失败等 8 项问题。
- 新增《双机热备只读纳管设计》，说明为何只做观测不做编排。

### ✅ 验证 (Verified)
- 真机双机验证（10.8.8.157 / 10.8.8.235）：HA 部署、主动切换（VIP 漂移 10s）、原主库自动重新加入（15s）、数据一致性、跨节点状态聚合。
- 记录一项需要在现场验收说明的既有行为：切换完成后业务恢复约 4.5 分钟，瓶颈在 HikariCP 连接池重建，非本工具可缩短。
- 性能：20 并发跨节点聚合 13894ms → 3422ms（冷）/ 760ms（热）；30 并发本机 66ms；约 1000 次请求 / 5 分钟内存稳定在 37–43MB。

### 🧹 维护 (Chore)
- 移除像素级视觉回归（BackstopJS 与 Playwright `toHaveScreenshot`），保留全部功能用例；验证门禁改为 `npm run check` + `npm run test:ui`。

## [2.1.1] - 2026-08-26

### 🐛 修复 (Fixed)
- 修复部署包不再下发 `conf/thingsboard.yml` 和 `conf/tb-edge.yml` 时，启动校验判定关键配置缺失并弹出阻断弹窗的问题。校验语义调整为“存在配置来源”，业务服务目录下的 `.env` 含真实配置即视为合法。
- 修复 YAML 查找会兜底到工具自身安装目录的问题。该行为会把工具开发期夹带的样例配置当作现场配置抽取初值，可能给边缘端部署灌入云端模板值。
- 部署诊断中的“YAML 模板”检查项不再对新版部署包常亮告警。

### ✅ 验证 (Verified)
- 补充新旧两种部署包形态和空 `.env`、无配置来源等故障场景的回归用例。

## [2.1.0] - 2026-08-26

### ✨ 新增 (Added)
- 新增 IoTDB 时序存储配置分组，共 24 个配置项：连接、数据保留、读路径、写路径、超时重试和 EDQS 保护开关。
- 历史数据存储类型和最新数据存储类型新增 `iotdb` 选项。
- IoTDB 纳入服务纳管，支持启动、停止、重启、日志查看和一键数据清理，操作方式与 PostgreSQL、Redis、Kafka 一致。
- 部署计划在任一存储类型选择 IoTDB 时自动纳入 IoTDB 依赖检查。
- 云端与边缘端共用同一份 IoTDB 配置定义，从结构上避免云边配置漂移。

### 🐛 修复 (Fixed)
- 修复表单对 `.env` 中缺失的配置项不回落到元数据默认值的问题。该问题会导致切换存储类型时新增配置项渲染为空值，保存后写出空串并注入容器，数字类配置绑定失败导致平台启动异常。

### 🔒 安全约束 (Security)
- 最新数据存储选择 IoTDB 时，强制锁定 EDQS 同步与查询接口为关闭状态，避免配置出无法启动的服务。

### ✅ 验证 (Verified)
- 补充云边配置一致性、依赖显隐、EDQS 守卫、字段默认值完备性、部署计划依赖推导和服务注册表回归用例。
- 通过单元测试、UI、视觉、Lint 和构建验证，未更新视觉基准截图。

## [2.0.6] - 2026-06-25

### 🐛 修复 (Fixed)
- 修复平台配置源码/表单切换和取消编辑后未正确丢弃未保存修改的问题。
- 修复服务数据清理弹窗展示的备份目录与后端实际备份目录不一致的问题。
- 修复配置搜索框清空后未恢复全部配置项的问题。
- 修复 Postgres/Redis 服务详情缺少 compose 其他配置的问题。
- 修正登录页默认密码修改路径说明。

### 🎨 优化 (Changed)
- 平台配置管理移除总览入口，配置项改为逐行展示。
- 配置校验弹窗移除立即重启按钮和中间统计卡片。
- 服务日志页面移除日志级别数量统计。
- 服务详情移除 CPU、内存和运行时长占位信息。

### ✅ 验证 (Verified)
- 补充服务清理备份目录、非法目录、过期目录、服务详情配置和状态码回归用例。
- 更新登录页和配置页移动端视觉基准。
- 通过 UI、视觉、类型、Lint、构建和定向单元测试验证。

## [2.0.4] - 2026-05-25

### 🐛 修复 (Fixed)
- 修复初始化安装执行中切换页面导致进度状态丢失的问题。
- 修复动作确认弹窗依赖慢接口刷新导致响应变慢的问题，安装、服务重启和保存并应用均改为即时弹出确认。
- 修复确认弹窗快速关闭后再次打开可能闪烁或被上一次关闭动作隐藏的问题。
- 修复 Docker 镜像未同步应用版本元数据，导致容器接口仍可能显示旧版本的问题。

### 🎨 优化 (Changed)
- 初始化安装执行期间锁定其他导航入口，并给动作按钮增加即时点击反馈和忙碌态提示。

### ✅ 验证 (Verified)
- 补充初始化安装锁导航、确认弹窗速度、保存并应用慢接口和服务操作锁定回归用例。
- 通过 UI、视觉和构建验证。

## [2.0.3] - 2026-05-22

### 🐛 修复 (Fixed)
- 修复初始化安装页在依赖服务未启动时仍显示“准备就绪”的状态误判。
- 修复初始化安装阶段计数始终停留在 `0 / 6` 的问题。
- 更新安装页脚本缓存版本，避免容器重建后浏览器继续加载旧逻辑。
- 稳定安装页视觉测试夹具，并补充依赖未就绪和阶段进度回归用例。

### ✅ 验证 (Verified)
- 通过 UI、视觉、构建和容器重建验证。

## [2.0.2] - 2026-05-21

### 🐛 修复 (Fixed)
- 修复 Edge 场景登录页服务类型和服务标识仍显示 Cloud/iotcloud 的问题。
- 修复无 hash 登录后默认进入服务管理而不是总览的问题。
- 修复退出登录后可能残留配置变更弹层、平台配置和初始化安装页面登录按钮显示“登录中”的问题。
- 修复 Cloud 未启用 Cassandra 时顶部配置页签和展示数量不同步的问题。
- 修复服务启停/重启、保存配置和保存并应用到服务可重复点击触发的问题。
- 修正 Edge Kafka 回填配置项注释，并优化登录密码框空态显示。

### ✅ 验证 (Verified)
- 更新移动端登录页视觉基准，覆盖密码空态从掩码改为占位提示的预期变化。
- 复测人工测试 PDF 中 10 条问题，确认已修复或未复现。

## [2.0.1] - 2026-05-21

### 🎨 优化 (Changed)
- 统一全局动作按钮颜色语义，按刷新/查看、保存、启动、重启、危险、关闭/取消等操作类型区分。
- 服务管理、平台配置管理、历史版本、日志、安装和弹窗中的动态按钮统一接入动作语义样式。
- 更新 UI 视觉基准，覆盖动作按钮颜色调整后的桌面端和移动端页面快照。

## [2.0.0] - 2026-05-21

### ⚠️ 破坏性调整 (Breaking)
- 数据清理不再自动停止或重启目标服务；必须先停止业务服务和目标服务后才能清理。
- 数据清理备份目录迁移到 `services/config-mate/backups`，不再写入安装包根目录 `.config-mate/backups`。
- 移除旧 UI 预览页和预览主题文件，发布包只保留实际业务控制台。

### ✨ 新增 (New)
- 重构服务管理、平台配置管理、总览和初始化安装的统一工作台布局。
- 平台配置管理新增配置校验、历史版本弹窗、保存配置和保存并应用到服务的交互语义。
- 数据清理入口统一为红色危险动作，并在后端校验目标服务运行状态。
- 增加系统登录、配置校验、数据清理和 UI 一致性相关测试覆盖。

### 🐛 修复 (Fixed)
- 修复刷新后页面状态短暂跳转的问题。
- 修复配置历史在无实际变更时仍生成记录的问题。
- 修复运行检查和历史版本关闭时的闪烁与布局跳动。
- 修复源码模式底部内容遮挡、关闭按钮样式不统一、页面字体和间距不一致等 UI 问题。

### 🔄 更新 (Updated)
- 清理未完成的通知中心入口和设计预览产物。
- 统一顶部操作区、保存区、弹窗和服务卡片的交互层级。

## [1.4.11] - 2026-01-26

### ✨ 新增 (New)


### 🐛 修复 (Fixed)
- fix(ui): 修复页面布局异常与源码模式显示问题 (705470f)
- fix: 修复日志崩溃与移动端布局，优化运行时管理 (9c6d29f)

### 🔄 更新 (Updated)


# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]
